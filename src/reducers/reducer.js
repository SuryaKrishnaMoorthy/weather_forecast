const INTIAL_STATE = {
  schemaUrl: "https://rebilly.github.io/ReDoc/swagger.yaml",
  schemas: []
};

const reducer = (state = INTIAL_STATE, { type, payload }) => {
  switch (type) {
    default:
      return state;
  }
};

export { reducer };
